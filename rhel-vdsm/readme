Starting the container

On the RHEL 7 Host :
Running
	modprobe dm-multipath;
	modprobe softdog;
	modprobe ebtables;
	modprobe ebtable_filter;
	modprobe ebt_ip;
	modprobe ebt_pkttype;

	docker run --rm --privileged --net=host -ti -e 'container=docker' -v /sys/fs/cgroup:/sys/fs/cgroup:rw docker;

Building
	docker build -t vdsmc .


Testing
	Checking libvirt
		virsh --connect qemu+tcp://<docker host>/system list --all

Connectiong to docker container
	docker inspect --format {{.State.Pid}} <container_name_or_ID>
	$(docker inspect --format {{.State.Pid}} $(docker ps | grep vdsmc | awk '{print $1}'))
 	nsenter  --mount --uts --net --pid --ipc --target $(docker inspect --format {{.State.Pid}} $(docker ps | grep docker | awk '{print $1}'))
